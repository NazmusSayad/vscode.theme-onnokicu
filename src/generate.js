const colors = require("./theme/colors")
const tokenColors = require("./theme/tokenColors")
const semanticHighlighting = require("./theme/semanticHighlighting.js")
const semanticTokenColors = require("./theme/semanticTokenColors")

module.exports = {
  colors,
  tokenColors,
  semanticHighlighting,
  semanticTokenColors,
}
